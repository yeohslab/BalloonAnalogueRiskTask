
## 实验结构（决策粒度 · 被试内 + 双 Block）

- **实验入口**：先填写**人口学信息**（`bart_demographics`）：被试编号（**主试分配**，正整数；可选由 URL `?subj=` 等预填）、**出生年月**（`<input type="month">`，精确到月）、**性别**（男 / 女 / 其他）；CSV 列 `birth_month`（`YYYY-MM`）、`gender`、`age_months`（自出生至施测当月的完整月数）。通过后进入特质问卷等后续流程。
- **实验开始前**（人口学之后、BART 指导语之前）：**特质问卷**（与《特质量表.md》一致：**20** 题，**1–4** 级），任务类型 `bart_trait_scale`，列 `trait_t01`～`trait_t20`，`block_index` 记为 `0`；`rt` 为整套问卷用时。
- 每名被试**先后完成两个部分**（高粒度「细」与低粒度「粗」），**顺序随机**：`high_first`（先细后粗）或 `low_first`（先粗后细），写入 `block_order`。
- **每部分 21 个气球**：橙、黄、蓝各 **7** 个，该 block 内试次顺序 **随机打乱**（三色在序列中近似均匀、随机分布）。
- **每部分总收益单独**：进入该部分前将累计总收益**置 0**。
- **压力量表**（与项目内《量表,md》一致：各 **20** 题，**1–4** 级按钮）：  
  - 第 1 部分开始前、第 1 部分结束后；  
  - **休息**屏（被试自定节奏，点击继续）；  
  - 第 2 部分开始前、第 2 部分结束后。  
  数据任务类型：`bart_stress_scale`，字段 `measure_phase`：`pre_block_1` | `post_block_1` | `pre_block_2` | `post_block_2`；`stress_s01`～`stress_s20` 对应量表第 1～20 题得分（1–4）；题干与评定说明见《量表,md》。
- **全部 BART 结束后**：先测焦虑/紧张与决策难度（各 1–10），任务类型 `bart_block_self_report`；再测《刺激评定量表.md》**2** 题（**1–4** 级：0.1 元与 5 元奖励的吸引力），任务类型 `bart_stimulus_rating`，列 `stim_attract_01`、`stim_attract_5`，`rt` 为两题合计用时。
- **被试编号**：仅用于归档；`subject_id` = `S{编号}`。编号在**首屏人口学**中由主试填写，亦可由 URL `?subj=` 等预填。

## 决策粒度操纵（金额）

- **低粒度（粗）单泵**：`$5.00`（`baseRewardPerEffectiveStep`）。
- **高粒度（细）单泵**：`$0.10` = `BASE / k`，**k = 50**（`granularityK`）。

| 粒度 | 单次点击金额 |
|------|----------------|
| 低粒度（粗） | `$5.00` |
| 高粒度（细） | `$0.10` |

爆炸规则与粒度无关，见下。

### 气球缩放

视觉缩放按**点击次数**线性增加；与爆炸随机过程独立。

## 颜色与爆炸上限 M

- 三色仅外观不同；**M 恒为 64**（`CONFIG.explosionCapM`）。

## 爆炸时机（均匀随机点）

**N ~ Uniform{1,…,M}**，\(M=64\)。第 \(k\) 次充气后若未爆，本步条件爆炸概率为 \(1/(M-k+1)\)。

代码：`explosion_model` = `uniform_1_M`；`risk_type` 存 **M**（恒为 64）。

## 指标与 CSV

结束页显示：编号、block 顺序、两部分的充气/爆炸/调整充气/部分收益及全程合并。

### CSV 主要列

**`task === bart_trial`**：`subject_number`，`design`（`within_subjects`），`block_index`（1 或 2），`block_order`，`granularity`，金额相关字段，`trial_index`（全程 1–42），`trial_index_in_block`（1–21），`risk_type`（64），`explosion_model`，充气与收益字段等。

**`task === bart_demographics`**：`birth_month`，`gender`，`age_months`，`rt`；其余任务列多为空。

**`task === bart_trait_scale`**：`trait_t01`～`trait_t20`，`rt`（整套 20 题合计用时）；压力列空；`block_index` 为 `0`。

**`task === bart_stress_scale`**：`measure_phase`，`stress_s01`～`stress_s20`，`rt`（整套 20 题合计用时）；特质列空；其余 BART 列多为空。

**`task === bart_block_self_report`**：`anxiety_rating`，`difficulty_rating`；`block_index` 可为 0 表示全程结束后的补充题；刺激评定列为空。

**`task === bart_stimulus_rating`**：`stim_attract_01`（0.1 元吸引力）、`stim_attract_5`（5 元吸引力），`rt`（两题合计用时）；题干与 1–4 说明见《刺激评定量表.md》；`block_index` 为 `0`。
